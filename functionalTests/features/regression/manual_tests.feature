Feature: Manual tests

  # Should check scroll up and down very quickly
  # Should check click next button very quickly
  # Should check load more than 300 products in this page
  # Should check physical Inventory that can't rotate or the data will lost
  # Should test that one drug archived at 18th, and archived drug shouldn't appear at mmia/via/physical inventory at next period
  # Should add check steps in mmia and via form and make sure all data in the form is 0 or disappear
